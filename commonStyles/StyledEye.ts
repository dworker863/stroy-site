import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import styled from 'styled-components';

export const StyledEye = styled(FontAwesomeIcon)`
  position: absolute;
  right: -25px;
  top: 50%;
  transform: translateY(-50%);
`;
